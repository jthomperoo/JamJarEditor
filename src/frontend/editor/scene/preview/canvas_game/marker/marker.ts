import Component from "jamjar/lib/component/component";

class Marker extends Component {
    public static readonly KEY = "marker";
    constructor() {
        super(Marker.KEY);
    }
}

export default Marker;